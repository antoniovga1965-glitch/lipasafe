{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd services/api-gateway && npm install"
  },
  "deploy": {
    "startCommand": "cd services/api-gateway && node src/server.js"
  }
}
